# Release notes: VOR-020 to VOR-024

- Site risk refresh and health monitoring now share one canonical calculation.
- Current document knowledge coverage no longer reports chunkless records as usable full-document evidence.
- Engineers evidence uses one site-scoped backend bundle instead of sequential query waves.
- Equipment tab changes preserve the real portal scroll position.
- Selected tab styling is portal-scoped and no longer depends on `!important`.
