# Quality gates

The batch must pass TypeScript, all source contracts, route smoke, production build, performance budgets, authenticated browser tests and responsive verification before merge. Production verification and 24 consecutive site-risk health passes remain required before final completion.
